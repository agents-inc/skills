# web-routing-react-router

Client-side routing with data APIs — loaders, actions, error boundaries, search params, nested layouts, and code splitting

## Installation

Add this plugin to your Claude Code configuration:

```json
{
  "plugins": ["web-routing-react-router"]
}
```

## Usage

This skill is automatically available when installed.

---

*Generated by Agents Inc. skill-plugin-compiler*
