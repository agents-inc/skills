# yaml-language-server: $schema=https://raw.githubusercontent.com/agents-inc/cli/main/src/schemas/metadata.schema.json
category: ai-patterns
slug: tool-use-patterns
domain: ai
author: "@vince"
displayName: Tool Use Patterns
cliDescription: Provider-agnostic patterns for LLM function calling
usageGuidance: >-
  Use when implementing LLM tool use -- tool definition schemas, the tool call
  loop pattern, parallel tool calls, error reporting back to the model,
  multi-step agent workflows, human-in-the-loop patterns, and tool choice
  control. Provider-agnostic -- shows generic TypeScript patterns, not
  provider-specific SDK code.
